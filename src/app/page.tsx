import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <Button className="my-10 bg-red-500 px-10 py-5">Click me</Button>
    </div>
  );
};

export default Home;
